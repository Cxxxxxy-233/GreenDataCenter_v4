# Design System

## Overview
GreenDataCenter 的视觉系统定位为“绿色科技风中的专业高级感”。
这不是消费级炫酷科技界面，也不是传统政企平台界面，而是为专业决策服务的高可信产品界面。
整体视觉应围绕三个关键词展开：秩序、能流、判断。

## Theme
默认采用浅底产品界面配合深色沉浸式重点场景。
大多数配置、列表、详情、报告页面使用高可读浅色主题，以支持长时间阅读和参数输入。
首页 Hero、关键工作流、生成进度、智能体协同过程等高叙事区域允许使用深色科技场景，以强化“系统正在协同推演”的感受。
深色区域用于建立氛围，浅色区域用于承载信息，两者必须共享同一套色相逻辑与材质语言。

## Color Strategy
采用 Committed 策略。
绿色是系统主导色，不只是点缀色，而是承担核心品牌识别、主按钮、关键状态和流程高亮。
青色作为辅助科技色，用于信息流、协同关系、次级高亮和部分可视化。
琥珀色只用于提醒、权衡、预算压力和流程中需要注意的能量节点，不能泛用。

## Color Palette
所有颜色建议转为 OKLCH 管理，以下为现阶段可直接使用的近似值。

### Core Neutrals
- `canvas`: `#f4fbf7`
- `surface`: `#fbfdfb`
- `panel`: `#ffffff`
- `panel-muted`: `#f1f7f3`
- `text-primary`: `#17201c`
- `text-secondary`: `#50615a`
- `text-muted`: `#7a8d85`
- `border-subtle`: `#d8e6dd`
- `border-strong`: `#b6ccc0`

### Brand Greens
- `green-50`: `#eefaf3`
- `green-100`: `#d8f3e3`
- `green-300`: `#78d8a0`
- `green-500`: `#18b26b`
- `green-600`: `#11945a`
- `green-700`: `#0d7748`

### Tech Cyans
- `cyan-100`: `#dff6f7`
- `cyan-300`: `#73d8df`
- `cyan-500`: `#16b8c4`
- `cyan-700`: `#0e8090`

### Signal Ambers
- `amber-100`: `#fff1d8`
- `amber-300`: `#f6c56a`
- `amber-500`: `#d99a27`

### Dark Stage Colors
- `stage-900`: `#081512`
- `stage-800`: `#0d201a`
- `stage-700`: `#123126`

## Typography

### Font Stack
- UI Sans: `Inter`, `PingFang SC`, `Microsoft YaHei`, sans-serif
- Data Mono: `JetBrains Mono`, `Consolas`, monospace

### Type Principles
- 页面标题要更有张力，体现系统级产品气质。
- 正文、表格、参数说明优先可读性，不走过细字重。
- 数值、评分、PUE、ROI、碳排放等关键指标应使用更稳的数字排版，避免视觉抖动。
- 正文最大阅读宽度控制在 70ch 左右。

### Suggested Scale
- `display`: 40/48, 700
- `h1`: 32/40, 700
- `h2`: 24/32, 650
- `h3`: 20/28, 600
- `body`: 14/22, 400
- `caption`: 12/18, 500
- `metric`: 28/32, 700

## Surfaces & Elevation
- 普通信息面板使用轻微边框 + 低阴影，不依赖重卡片感。
- 高价值模块使用轻微色温偏移，而不是堆叠更多卡片。
- 深色工作流区使用柔和辉光和层叠雾感，但避免玻璃拟态常态化。
- 悬停抬升控制在 2px 到 4px，强调精度，不强调夸张运动。

## Shape Language
- 圆角偏中大，建议 10 / 14 / 18 三档。
- 面板形态更偏“仪表板结构件”而不是“营销卡片”。
- 节点、流程块、数值摘要块要有统一轮廓，不要每种模块都用不同边框语言。

## Layout
- 首页采用“沉浸式 Hero + 任务入口 + 最近项目 + 快速入门”的产品型首页结构。
- 工作流页面应突出中心路径，减少无关的漂浮模块。
- 生成页面应把“当前阶段、专家状态、共识度、关键结果”作为第一视觉层。
- 详情与报告页面应强化可扫描结构，优先结论、指标、权衡、建议。
- 避免全站每块都包在同样的白卡片里，应通过背景层、分区节奏和留白建立层级。

## Components

### Buttons
- Primary: 绿色实底，承担主要行动，如“开始规划”“生成方案”“查看详情”
- Secondary: 细边框或轻底色，用于次级操作
- Ghost: 用于表格和上下文动作
- Danger: 仅用于删除、取消等高风险行为

### Workflow Nodes
- 节点是本产品核心识别组件。
- 状态包含 `waiting`、`running`、`completed`、`warning`、`failed`。
- 每个状态都必须同时通过颜色、图标和标签变化体现。
- 节点之间的关系线应体现“能流”和“协同推演”，但不能喧宾夺主。

### Expert Panels
- 专家卡片应强调角色差异、结论摘要、关键指标、关注事项。
- 经济、可靠性、环保三个专家可有微差色，但结构必须统一。
- 置信度、评分、建议需形成固定信息顺序，便于横向对比。

### Metrics
- 所有关键指标组件应支持数值、单位、趋势、上下文说明。
- 避免单纯“大数字 + 小标题”模板，优先让指标与当前决策上下文绑定。

### Logs
- 日志区采用深底浅字，但信息密度要受控。
- 不同日志级别除了颜色，还应有关键词前缀或图标辅助识别。

## Data Visualization
- 绿色表示系统推荐、达标、优化收益。
- 青色表示过程流、协同、信息通道。
- 琥珀色表示权衡、预算压力、待确认。
- 红色仅用于失败、错误、严重风险。
- 图表背景保持干净，不使用高饱和大面积底色。

## Motion
- 动画基调是“流动、推演、渐进出现”，不是跳跃和炫技。
- 流程线、粒子、节点光晕只在重点区域使用。
- 所有动画使用 ease-out-quart 或接近的缓出曲线。
- 避免无限吸引注意力的强闪烁、强脉冲和高频发光。
- 对长时间使用场景，应支持低动效策略。

## Imagery & Atmosphere
- 不使用传统风电光伏宣传照作为主视觉。
- 如果需要插图，应偏抽象系统图、能流结构、数据中心设施空间感，而不是通用素材图。
- 视觉重点是“智能协同中的基础设施决策”，不是“新能源宣传海报”。

## Copy Tone
- 用语应专业、克制、解释充分。
- 避免夸张宣传语、空泛 AI 术语堆砌和不必要的口号化表达。
- 标题偏结论化，说明偏工程化，按钮偏行动导向。

## Accessibility
- 关键文本与背景满足高对比。
- 关键状态不只靠颜色区分。
- 表格、日志、工作流状态在长时间浏览下仍保持可读。
- 深色场景中的绿色和青色发光必须受控，避免影响阅读。
